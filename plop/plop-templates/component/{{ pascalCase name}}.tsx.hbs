import React from 'react';
import { {{ pascalCase name }}Props } from './{{ camelCase name }}.types';
import * as S from './{{ camelCase name }}.styled';

{{#if forwardRef}}
export const {{ pascalCase name }} = React.forwardRef<HTMLDivElement, {{ pascalCase name }}Props>(({ className }, ref) => {
  return (
    <S.{{ pascalCase name }} className={className} ref={ref}>
     {{ pascalCase name }}
    </S.{{ pascalCase name }}>
  );
});

{{ pascalCase name }}.displayName = '{{ pascalCase name }}';
{{else}}
const {{ pascalCase name }} = ({ className }: {{ pascalCase name }}Props) => {
  return (
    <S.{{ pascalCase name }} className={className}>
     {{ pascalCase name }}
    </S.{{ pascalCase name }}>
  );
};
{{/if}}

export default {{ pascalCase name }};
